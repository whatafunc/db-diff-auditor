DB_DRIVER=mysql
DB_HOST=localhost
DB_DATABASE=mydatabase
DB_USERNAME=root
DB_PASSWORD=
DB_PORT=3306

# Optional: path to the snapshots directory
DB_SNAPSHOT_PATH=.db-snapshots

# Optional: comma-separated list of tables to ignore
DB_IGNORE_TABLES=migrations,sessions
